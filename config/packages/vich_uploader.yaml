vich_uploader:
    db_driver: orm

    mappings:
        document_images:
            uri_prefix: '%app.upload.document%'
            namer: Vich\UploaderBundle\Naming\UniqidNamer
            upload_destination: '%kernel.project_dir%/public%app.upload.document%'