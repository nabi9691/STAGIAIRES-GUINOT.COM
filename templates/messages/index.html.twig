{% extends 'base.html.twig' %}

{% block title %} Liste des messages! {% endblock %}


{% block body %}

{% block content %}
<div>
    <h1>
     bienvenu(e) !
     Messagerie de Guinot :
     </h1>

    <br /><br />
    <hr />
    <br />
    <a href="{{ path('nouveauMessage_index') }}">Nouveau message </a>
<br /><br />
    
    <table border="1">
            <thead>
                <tr>
                    <th> Id </th>
                    <th> Titre du message </th>
                    <th> Contenu du message </th>
                    <th> Creer date </th>
                    <th> Si le message est lu </th>
                    <th> Message envoyer </th>
                    <th> Message reçu </th>
                    <th> Utilisateur </th>
                    <th> Modifier </th>
<th> Afficher </th>
<th> Supprimer </t  h>
</tr>
            </thead>
            {% for message in message %}
<tbody>
<tr>
                                <td> {{ message.id }} </td>
<td> {{ message.titre_message }} </td>
                <td> {{ message.contenu_message }} </td>
                <td> {{ message.creer_date|date("d/m/Y") }} </td>
<td> {{ message.si_messageLu }} </td>
                <td> {{ message.message_envoyer }} </td>
                <td> {{ message.message_reçu }} </td>
                                <td> {{ message.utilisateur }} </td>
                                <td><a href="{{ path('modifierMessage_index', {'id': message.id }) }}">Modifier</a></td>
<td><a href="{{ path('afficherMessage_index', {'id': message.id }) }}">afficher</a></td>
<td><a href="{{ path('supprimerMessage_index', {'id': message.id }) }}">Supprimer</a></td>
                </tr>
                {% else %}
                <tr>
                <td colspan="5">
                    Vous n'avez pas de messages enregistré dans votre base de données !
                </td>
            </tr>
            {% endfor %}
        </tbody>
    </table>

</div>
{% endblock %}

<br/><br/>

{% block footer %}
<p>
pied de page !
</p>
{% endblock %}
{% endblock %}
